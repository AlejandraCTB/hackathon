/*
  Firebase Web App config.

  1. Go to Firebase Console > Project settings > General.
  2. Create or select a Web App.
  3. Copy the firebaseConfig object.
  4. Paste the real values below.

  These values identify your Firebase project in the browser. They are not admin
  secrets, but you must protect the database with Firestore Security Rules.
*/

export const firebaseConfig = {
  apiKey: "REPLACE_WITH_API_KEY",
  authDomain: "REPLACE_WITH_AUTH_DOMAIN",
  projectId: "REPLACE_WITH_PROJECT_ID",
  storageBucket: "REPLACE_WITH_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_WITH_MESSAGING_SENDER_ID",
  appId: "REPLACE_WITH_APP_ID"
};
